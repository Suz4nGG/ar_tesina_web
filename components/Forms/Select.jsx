const Select = () => {
  return (
    <div>
      <label>
        Tipo de discapacidad
      </label>
      <select>
        <option>{optText}</option>
      </select>
    </div>
  );
}

export default Select;
